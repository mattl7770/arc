-- ARC seed data.
--
-- Runs against a local `supabase start` database after migrations. Keep this
-- limited to reference data that is the same for every environment — the
-- biomarker catalogue described in docs/data-model.md is the main candidate.
--
-- Never put personal health data here. This file is committed to git.

-- No seed data yet: the v1 schema has not landed. See docs/data-model.md.
