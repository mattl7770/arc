import { Placeholder } from '@/components/ui/placeholder';
import { Screen } from '@/components/ui/screen';

export default function SettingsScreen() {
  return (
    <Screen>
      <Placeholder
        title="Settings"
        purpose="Account, integrations, protocol management, and data export."
      />
    </Screen>
  );
}
