node_modules/
.expo/
dist/
android/
ios/
package-lock.json

# Agent worktrees. Each holds a full checkout of a different branch, so
# `prettier --check .` reports that branch's files as failures and the gate
# becomes unreadable. They are not part of this tree's source.
.claude/worktrees/

# Markdown is hand-authored and is the project's source of truth
# (CLAUDE.md + /docs). Prettier reflows tables and wrapping, which would churn
# the diff on files people actually read. Format prose by hand.
*.md

# Generated by `npm run db:types`. Formatting it would just be undone by the
# next regeneration and make format:check fail in CI.
src/types/database.ts

# Generated by `npm run db:bundle` from db/migrations/*.sql. Prettier would
# reformat it, desyncing it from the bundler output and failing `db:validate`
# (which byte-compares the two). The .sql sources are the thing to read.
src/lib/db/migrations.generated.ts

# Generated by expo (regenerated on every `expo start`).
expo-env.d.ts
